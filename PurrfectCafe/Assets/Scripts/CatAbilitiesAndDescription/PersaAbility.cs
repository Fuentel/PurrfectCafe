using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PersaAbility : CatAbility
{
    // Start is called before the first frame update
    void Start()
    {
        DescriptionCat = "Description: The royal kittens of the café, you still don´t know if this cat belong to you or if you belong to this cat, but they are to cute to even think about that.";
        DescriptionCatAbility = "Ability: None :(";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
