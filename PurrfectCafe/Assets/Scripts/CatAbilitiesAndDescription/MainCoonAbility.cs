using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainCoonAbility : CatAbility
{
    // Start is called before the first frame update
    void Start()
    {
        DescriptionCat = "Description: Okay now these cats are getting out of control, where are you even supposed to put a cat which is the same size as you.";
        DescriptionCatAbility = "Ability: None :(";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
