using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SiamesAbility : CatAbility
{
    // Start is called before the first frame update
    void Start()
    {
        DescriptionCat = "Description: Despite the heart this fellow kitten has in its face, this little creature is known to be very hostile with new people.";
        DescriptionCatAbility = "Ability: None :(";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
