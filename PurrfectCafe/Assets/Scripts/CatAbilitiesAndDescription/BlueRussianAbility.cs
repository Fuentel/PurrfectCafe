using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlueRussianAbility : CatAbility
{
    // Start is called before the first frame update
    void Start()
    {
        DescriptionCat = "Description: Blue Russian cats are known to be extremely fluffy, be careful if you touch them because you could be trap there forever.";
        DescriptionCatAbility = "Ability: None :(";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
