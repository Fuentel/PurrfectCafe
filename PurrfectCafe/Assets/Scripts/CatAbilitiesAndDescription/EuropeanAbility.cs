using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EuropeanAbility : CatAbility
{
    // Start is called before the first frame update
    void Start()
    {
        DescriptionCat = "Description: The European cat is very common in European like its name suggest.";
        DescriptionCatAbility = "Ability: Steve Ability";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
